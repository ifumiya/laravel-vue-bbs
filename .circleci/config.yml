version: 2.1

jobs:
  build:
    docker:
      - image: circleci/php:7.3
    working_directory: ~/project
    steps:
      - checkout
      - run: echo 'banana'
      - run: composer install
      - run: php artisan key:generate
  test:
    docker:
      - image: php/7.3
    working_directory: ~/project
    steps:
      - checkout

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
